module.exports = function() {

  this.up = function(schema) {
    return schema.create('%{table}', {
      id: {
        dataType: 'INT',
        nullable: false,
        autoIncrement: true,
        primaryKey: true
      }
    });
  };

  this.down = function(schema) {
    return schema.drop('%{table}');
  };

}
