import { Schema, Column } from 'pretty-api/database'

export default class {
  up() {
    return Schema.table('%{table}').create([
      Column.increment('id')
    ]).primaryKeys(['id'])
  }

  down() {
    return Schema.table('%{table}').drop()
  }
}
